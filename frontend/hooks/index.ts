/**
 * Custom React hooks.
 *
 * Hooks abstract stateful logic away from UI components.
 * Follows CODING_STANDARDS §7: never call APIs directly inside components.
 *
 * Future hooks (Milestone 2+):
 *   useAuth()          ← Authentication state
 *   useCurrentUser()   ← Current user profile
 *   useNotifications() ← Real-time notifications
 */

export {};

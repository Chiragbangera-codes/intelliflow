/**
 * Shared utility functions.
 *
 * Pure, stateless utility functions used across features.
 * Functions must be unit-testable with no side effects.
 *
 * Future utilities (Milestone 2+):
 *   utils/
 *     cn.ts         ← Class name merging (clsx + tailwind-merge)
 *     format.ts     ← Date, currency, number formatting
 *     validation.ts ← Shared Zod schemas
 */

export {};
